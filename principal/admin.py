# encoding: utf-8
from django.contrib import admin
from principal.models import Cliente

# Register your models here.
#Página para gestionar a los clientes en administración
admin.site.register (Cliente)